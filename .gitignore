# secrets — never commit
.env
.env.*
!.env.example

# node / next
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# go
/engine/engine
/engine/*.exe
/engine/bin/
/engine/cache/

# os / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

.vercel

# local-only, not published to the repo (root-anchored so the web/public/ copies
# the app actually serves are unaffected)
/CLAUDE.md
/image_1.jpg
/image_2.webp
/image_3.png
